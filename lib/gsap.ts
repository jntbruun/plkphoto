/**
 * Register GSAP plugins once at module level.
 * Import this in any component that uses GSAP.
 */
import { gsap } from "gsap";

// No paid plugins needed for this project.
// Registered here so any component can safely import without double-registering.

export { gsap };
